version: 2

models:
  - name: bronze_sales #  Must match the filename of a model -- including case sensitivity.
    config:
        materialized: view
      

  - name: bronze_date
    config:
      materialized: view

  - name: bronze_store
    config:
        materialized: view